<%= notice %> 
<%= form_for @article, :url => article_path, :method => 'put'  do|f| %> 
  <%= f.text_field :title %>
  <%= f.text_area :content %>
  <%= text_field_tag :for_tags %>
  <%= f.submit :class => 'btn-primary'  %>
<%end%>
