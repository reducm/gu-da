<nav class="navbar navbar-inverse navbar-fixed-top" id="navbar" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="<%= logo_link %>" class="navbar-brand">
        <i class="logo_tiny"></i>
        <span class='gu'>咕</span>哒
      </a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right navtool">
        <% unless @user_name.blank? %>
          <li id="notification_li"><%= get_notifications_count(@user_id) %></li>
          <% if logined? %>
            <li> <a href="<%= new_article_path() %>"><i class="icon-edit"></i></a> </li>
          <% end %>

            <% if @current_user && @current_user.id %>
              <li><%= ( link_to  raw("<i class='icon-rss'></i>"), user_articles_path(@current_user)+".atom", class: 'rss' ) %></li>
            <% end %>

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <i class="icon-cogs"></i>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to '设置', edit_user_path(@user_id) %> </li>
              <li><%= link_to "关于", pages_path %></li>
              <li><%= link_to "退出", logout_path %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to '登录', blog_path %> </li>
          <li><%= link_to '注册', new_user_path %> </li>
        <%end%>
      </ul>
    </div>
  </div>
</nav> 
